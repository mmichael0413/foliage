<div class="fixture-item">
    <img class="fixture-image" src="{{image}}" alt="{{name}}" onerror="this.src='{{imageError}}'"></img>
    <div class="fixture-breakdown">
        <p><span class="label">Name:</span>{{name}}</p>
        <p><span class="label">Total:</span>{{total}}</p>
        <p><span class="label">Have Problems:</span>{{matching}}</p>
        <p><span class="label">Stores:</span>{{stores}}</p>
    </div>
</div>