{{#rows}}
    <div class="item pure-g">
        <div class="col-1-3">
            <a href="{{links.profile}}">{{name}}</a>
        </div>
        <div class="col-1-3">
            {{address}}
        </div>
        <div class="col-1-3">
            {{status}}
        </div>
    </div>
{{else}}
    <p class="item">There are no stores for this query.</p>
{{/rows}}