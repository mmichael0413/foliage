<div class="widget {{config.widget_class}}">
    <p>{{{title}}}</p>
    <ul class="heatmap-legend">
        {{#each config.legend}}
            <li><i class="fa fa-square" style="color: {{this}}"></i> {{@key}}</li>
        {{/each}}
    </ul>
    <div class="heatmap"><svg></svg></div>
</div>