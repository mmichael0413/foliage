<section class="sales-widget pure-g">
  <p class="text col-2-3">
  {{#if message}}
    {{message}}
  {{else}}
    QTD $ Sales: <span class="percentage-change {{ percentageChangeClass salesChange }}"><i class="ic {{percentageChangeIcon salesChange }}"></i> {{formatPercentageChange salesChange}}</span> {{#if showLabel}} From LY{{/if}}
  {{/if}}
  </p>
  <a class="details col-1-3" href="{{salesUrl}}">DETAILS</a>
</section>