<h1>Surveys</h1>
<div class="section">
    <table>
        <thead>
        <tr>
            <th>Name</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {{#if surveys}}
        {{#each surveys}}
            <tr>
                <td><a href="programName/survey/build/{{this.id}}">{{this.title}}</a></td>
                <td>
                    <a href="programName/survey/build/{{this.id}}">Edit</a>
                    <a href="programName/survey/build/{{this.id}}">Delete</a>
                </td>
            </tr>
        {{/each}}
        {{else}}
            <tr><td>No Surveys</td><td></td></tr>
        {{/if}}
        </tbody>
    </table>
</div>







