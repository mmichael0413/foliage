<a href="{{link}}" class="nav-item {{#if active}}active{{/if}} {{className}}">
    <i class="ic {{icon}}"></i>
    <span class="link">{{title}}</span>
    {{#if icon2}}
        <i class="ic {{icon2}}"></i>
    {{/if}}
</a>