<p class="item col-1-4">{{title}}</p>
<p class="item col-1-4">{{dateRange}}</p>
<p class="item col-2-4">
    <a href="{{links.self}}" class="btn primary">Scheduling Progress</a>
    <a href="{{links.progress}}" class="btn primary">Visit Progress</a>
</p>