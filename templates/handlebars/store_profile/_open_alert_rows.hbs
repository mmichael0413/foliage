{{#rows}}
	<div class="item pure-g">
		<p class="col-6-12 col-md-1-2">{{title}}</p>
		<p class="col-4-12 col-md-1-2">{{created_at}}</p>
		<div class="col-1-12">
			<a href="{{links.resolve}}" class="btn primary resolve"><i class="ic fa ic_check"></i></a>
		</div>
		<div class="col-1-12">
			<a href="{{links.checkin}}" class="btn primary"><i class="ic fa ic_report-16"></i></a>
		</div>
	</div>
{{else}}
	<p>There are no Open Alerts for this Store</p>
{{/rows}}